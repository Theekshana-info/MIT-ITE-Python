# 🏗️ Coding Challenges System Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                         App.tsx                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              PyodideProvider (Context)                 │  │
│  │  - Loads Pyodide once for entire app                  │  │
│  │  - Provides runPython() function                      │  │
│  │  - Manages loading state                              │  │
│  └───────────────────────────────────────────────────────┘  │
│                            │                                 │
│  ┌─────────────────────────▼───────────────────────────┐   │
│  │                     Router                           │   │
│  │  - /data-types-challenges → DataTypesChallenges     │   │
│  │  - /operators-challenges → (future)                 │   │
│  │  - /loops-challenges → (future)                     │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow

```
┌────────────────────────────────────────────────────────────┐
│  Step 1: Define Challenges                                 │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  client/src/data/dataTypeChallenges.ts               │ │
│  │                                                       │ │
│  │  export const dataTypeChallenges = [                │ │
│  │    { id, title, description, starterCode, ... }     │ │
│  │  ]                                                   │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────┐
│  Step 2: Page Component                                    │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  client/src/pages/DataTypesChallenges.tsx           │ │
│  │                                                       │ │
│  │  - Imports dataTypeChallenges array                 │ │
│  │  - Manages selected challenge state                 │ │
│  │  - Shows list or detail view                        │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
                            │
            ┌───────────────┴───────────────┐
            ▼                               ▼
┌───────────────────────┐    ┌─────────────────────────────┐
│  List View            │    │  Detail View                │
│  ┌─────────────────┐  │    │  ┌───────────────────────┐ │
│  │ ChallengeList   │  │    │  │  ChallengeEditor      │ │
│  │                 │  │    │  │                       │ │
│  │ - Grid layout   │  │    │  │  - Monaco Editor      │ │
│  │ - Easy/Med/Hard │  │    │  │  - Run button         │ │
│  │ - Click to open │  │    │  │  - Output area        │ │
│  └─────────────────┘  │    │  │  - Show solution      │ │
└───────────────────────┘    │  │  - Reset button       │ │
                              │  │                       │ │
                              │  │  Uses:                │ │
                              │  │  usePyodide() hook    │ │
                              │  └───────────────────────┘ │
                              └─────────────────────────────┘
```

## Component Hierarchy

```
DataTypesChallenges
    │
    ├─── Header
    │     ├─── Title
    │     └─── Description
    │
    ├─── Introduction Card
    │     └─── "How It Works" steps
    │
    ├─── Stats Cards
    │     ├─── Total
    │     ├─── Easy
    │     ├─── Medium
    │     └─── Hard
    │
    └─── Content (conditional)
          │
          ├─── ChallengeList (if no selection)
          │     └─── Challenge Cards (grouped by difficulty)
          │           ├─── Easy Section
          │           ├─── Medium Section
          │           └─── Hard Section
          │
          └─── ChallengeEditor (if challenge selected)
                ├─── Back Button
                ├─── Challenge Header
                │     ├─── Title
                │     ├─── Description
                │     └─── Difficulty Badge
                │
                ├─── Hints Alert (optional)
                │
                ├─── Code Editor Card
                │     ├─── Toolbar
                │     │     ├─── Reset Button
                │     │     └─── Run Button
                │     └─── Monaco Editor
                │
                ├─── Output Card (conditional)
                │     ├─── Success/Error Icon
                │     └─── Output/Error Text
                │
                ├─── Expected Output Card (optional)
                │
                └─── Solution Card
                      ├─── Show/Hide Button
                      └─── Solution Editor (read-only)
```

## Python Execution Flow

```
┌────────────────────────────────────────────────────────────┐
│  User clicks "Run Code"                                    │
└────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────┐
│  ChallengeEditor.tsx                                       │
│  - Gets code from Monaco Editor                            │
│  - Calls: const { runPython } = usePyodide()              │
└────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────┐
│  PyodideContext.tsx                                        │
│  - Checks if Pyodide is loaded                            │
│  - Captures stdout/stderr setup                            │
│  - Executes: await pyodide.runPythonAsync(code)           │
│  - Returns: { stdout: string, stderr: string }            │
└────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────┐
│  ChallengeEditor.tsx                                       │
│  - Receives { stdout, stderr }                            │
│  - Displays in Output Card                                 │
│  - Shows success or error styling                          │
└────────────────────────────────────────────────────────────┘
```

## File Structure

```
client/src/
│
├── types/
│   └── challenges.ts                 # Type definitions
│
├── contexts/
│   └── PyodideContext.tsx            # Python execution provider
│
├── components/
│   ├── ChallengeList.tsx             # Grid view of challenges
│   ├── ChallengeEditor.tsx           # Single challenge detail
│   ├── AppSidebar.tsx                # ✏️ Modified (added nav link)
│   └── Navbar.tsx                    # ✏️ Modified (added quick access)
│
├── pages/
│   └── DataTypesChallenges.tsx       # Main challenges page
│
├── data/
│   └── dataTypeChallenges.ts         # Challenge content
│
└── App.tsx                            # ✏️ Modified (added route + provider)
```

## State Management

```
DataTypesChallenges Component
    │
    └─── State: selectedChallenge
          │
          ├─── null → Show ChallengeList
          │
          └─── CodingChallenge → Show ChallengeEditor
                    │
                    └─── State: code (user's code)
                    └─── State: output (execution result)
                    └─── State: error (execution error)
                    └─── State: showSolution (boolean)
```

## Pyodide Context State

```
PyodideProvider
    │
    ├─── State: pyodide (Pyodide instance | null)
    ├─── State: isLoading (boolean)
    └─── State: error (string | null)
    │
    └─── Provides:
          ├─── pyodide: any
          ├─── isLoading: boolean
          ├─── error: string | null
          └─── runPython: (code: string) => Promise<{stdout, stderr}>
```

## Type Structure

```typescript
// Type hierarchy
DifficultyLevel = "easy" | "medium" | "hard"
    │
    ▼
CodingChallenge {
  id: string
  title: string
  description: string
  starterCode: string
  solutionCode: string
  topicTag: string
  level: DifficultyLevel
  hints?: string[]
  expectedOutput?: string
}
    │
    ▼
ChallengeSet {
  topicName: string
  topicSlug: string
  description: string
  challenges: CodingChallenge[]
}
```

## Navigation Integration

```
┌────────────────────────────────────────────────────────────┐
│  Desktop Navigation (AppSidebar.tsx)                       │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  • Home                                              │ │
│  │  • Data Types                                        │ │
│  │  • Data Types Challenges  ← NEW                     │ │
│  │  • Operators                                         │ │
│  │  • Conditionals                                      │ │
│  │  • Loops                                             │ │
│  │  • Functions                                         │ │
│  │  • Arrays / Lists                                    │ │
│  │  • NumPy                                             │ │
│  │  • Flowcharts                                        │ │
│  │  • Code Editor                                       │ │
│  │  • Activities                                        │ │
│  │  • Summary                                           │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│  Mobile Navigation (Navbar.tsx)                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Quick Access Bar (horizontal scroll):               │ │
│  │  [Data Types] [DT Challenges] [Operators] [...]     │ │
│  │       ↑              ↑                                │ │
│  │     existing        NEW                               │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

## Reusability Pattern

```
To add a new topic (e.g., Operators):

Step 1: Create data file
┌────────────────────────────────────────────┐
│ client/src/data/operatorsChallenges.ts    │
│                                            │
│ export const operatorsChallenges = [...]  │
└────────────────────────────────────────────┘

Step 2: Create page (copy DataTypesChallenges)
┌────────────────────────────────────────────┐
│ client/src/pages/OperatorsChallenges.tsx  │
│                                            │
│ import { operatorsChallenges } from ...   │
│ // Same structure as DataTypesChallenges  │
└────────────────────────────────────────────┘

Step 3: Add route
┌────────────────────────────────────────────┐
│ App.tsx                                    │
│                                            │
│ <Route path="/operators-challenges"       │
│   component={OperatorsChallenges} />      │
└────────────────────────────────────────────┘

Step 4: Add navigation
┌────────────────────────────────────────────┐
│ AppSidebar.tsx & Navbar.tsx                │
│                                            │
│ { title: "Operators Challenges",          │
│   url: "/operators-challenges" }          │
└────────────────────────────────────────────┘

✅ Done! All components are reused automatically.
```

## User Journey

```
1. User visits /data-types-challenges
    ↓
2. Sees list of challenges (grouped by difficulty)
    ↓
3. Clicks a challenge card
    ↓
4. ChallengeEditor opens with:
   - Challenge description
   - Pre-filled starter code
   - Run button
    ↓
5. User writes/modifies code
    ↓
6. User clicks "Run Code"
    ↓
7. Pyodide executes Python
    ↓
8. Output displays below editor
    ↓
9. If stuck, user clicks "Show Solution"
    ↓
10. Solution appears (separate from user code)
    ↓
11. User clicks "Back to Challenges" or tries another
```

## Technology Stack

```
┌─────────────────────────────────────────────────────┐
│  Frontend Framework                                 │
│  • React 18.3.1                                    │
│  • TypeScript 5.6.3                                │
│  • Vite 5.4.20 (build tool)                        │
└─────────────────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────────────────┐
│  UI Components                                      │
│  • shadcn/ui (Card, Button, Badge, Alert, etc.)   │
│  • Tailwind CSS 3.4.17                             │
│  • Lucide React (icons)                            │
└─────────────────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────────────────┐
│  Code Editor                                        │
│  • Monaco Editor (VS Code engine)                  │
│  • Python syntax highlighting                      │
│  • Auto-complete                                    │
└─────────────────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────────────────┐
│  Python Execution                                   │
│  • Pyodide v0.24.1                                 │
│  • Python 3.11 (WebAssembly)                       │
│  • Runs in browser                                  │
│  • No backend needed                                │
└─────────────────────────────────────────────────────┘
```

## Performance Considerations

```
Optimization Strategy:
┌────────────────────────────────────────────────┐
│  1. Pyodide loads once at app startup          │
│     - Shared via React Context                 │
│     - ~10MB download (cached by browser)       │
│     - 2-3 second initial load                  │
└────────────────────────────────────────────────┘
                    │
┌────────────────────────────────────────────────┐
│  2. Monaco Editor lazy-loads                   │
│     - Only when challenge page opens           │
│     - ~5MB (cached)                            │
└────────────────────────────────────────────────┘
                    │
┌────────────────────────────────────────────────┐
│  3. Challenge data is static                   │
│     - No API calls                             │
│     - Imports directly                         │
│     - Minimal memory footprint                 │
└────────────────────────────────────────────────┘
```

## Summary

This architecture provides:
- ✅ Complete separation of concerns
- ✅ Reusable components
- ✅ Type safety
- ✅ Efficient Python execution
- ✅ Scalable to multiple topics
- ✅ Maintainable code structure
- ✅ Great user experience
